spring:
  application:
      name: service-product
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      config:
        import-check:
         enabled: false # 关闭nacos配置导入检查


server:
  port: 9000

